# Veeva Iconfont

将以下文件放入此目录，图标预览页即可正常显示：

- `iconfont.css`  — @font-face + .veev-xxx 类名
- `iconfont.js`   — SVG Symbol sprite
- `iconfont.ttf` / `iconfont.woff` / `iconfont.woff2` — 字体文件

来源：iconfont.cn 项目 ID 291740，font-family: `veev`，类名前缀 `.veev-`
